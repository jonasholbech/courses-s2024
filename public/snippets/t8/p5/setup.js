// windowWidth
// windowHeight
function setup() {
  const canvas = createCanvas(400, 400);
  // Move the canvas so it’s inside
  // <div id="sketch-holder">.
  canvas.parent("sketch-holder");
  frameRate(60);
}
